# Handoff — 2026-08-17

**Branch:** `main`
**HEAD:** `ddac218` — `feat: implement notification unread badge (Step 18.4)`
**Working tree:** clean (chưa push lên origin/main)

## Trạng thái dự án

- Feature checkpoint: **Step 18.4 — Notification unread badge (header)** đã hoàn thành và commit.
- Chuỗi commit gần nhất:
  - `ddac218` feat: implement notification unread badge (Step 18.4)
  - `e906f2f` update (docs meeting note 2026-08-16)
  - `3feb8c9` feat: implement role-based dashboard (Step 18.3)
- Backend KHÔNG thay đổi trong Step 18.4 — hoàn toàn frontend.

## Phiên vừa rồi (Step 18.4)

Tuân thủ quy trình: audit trước (schema/usecase/domain/implementation), đề xuất plan, chờ người dùng xác nhận, rồi mới code + commit. Người dùng yêu cầu: không tự ý code, không tự ý commit, không mở rộng scope.

**Thay đổi (4 file, frontend only):**
- `frontend/src/features/notifications/useUnreadNotifications.ts` (mới) — hook fetch count chưa đọc: fetch lúc mount + poll 60s, skip khi chưa đăng nhập/preview, cleanup + chống setState sau unmount, **API failure → setCount(0) (ẩn badge)**.
- `frontend/src/components/layout/Topbar.tsx` — badge trên button Bell (hiện khi count > 0, cap `99+`, `aria-label` kèm số).
- `frontend/src/components/layout/Topbar.module.css` — style badge.
- `frontend/src/__tests__/notifications.test.tsx` (mới) — **9 test**.

**Kiểm tra:** 33/33 tests PASS (foundation 4 + auth 11 + dashboard 9 + notifications 9), lint PASS, build PASS. `git diff --check` sạch.

**Lưu ý kỹ thuật đã vỡ:** test poll với `vi.useFakeTimers()` — `waitFor` không tự advance Vitest fake timers → test chết (timeout). Cách đúng: dùng `vi.advanceTimersByTimeAsync(UNREAD_POLL_INTERVAL_MS)` rồi thêm `await vi.advanceTimersByTimeAsync(0)` để React commit re-render. Chỉ bật fake timers ở test cần poll, các test còn lại dùng real timers + `waitFor`.

## Quy ước bắt buộc (từ project-progress.md + AGENTS.md)

- Đọc trước khi viết; tôn trọng ADR; dùng glossary domain.
- Backend patterns: `ApiResponse<T>`, `GlobalExceptionHandler`, phân quyền trong service layer, response DTO `fromEntity()`, `@AuthenticationPrincipal User currentUser`, test unit + MockMvc/H2.
- Notification module: contract CHỈ 7 method; KHÔNG có `findAllSent()` / `GET /api/notifications/sent`; không thêm matcher SecurityConfig cho `/api/notifications/**`.
- Seed: chỉ sửa `DataSeeder.java`, giữ `@Profile("seed")` + `@Transactional` + idempotent guard.
- Script test backend: `.\gradlew.bat clean test --console=plain` (trong `backend/`).
- Seed: `.\gradlew.bat bootRun --args="--spring.profiles.active=seed"`.

## Các hạng mục còn lại chờ người dùng xác nhận

1. **Charts + summary endpoint** — cần endpoint mới ở backend (đụng domain/controller/DTO). Cần audit schema/usecase trước.
2. **AI Chat / FAQ** — chưa có entity (`chat_conversations`, `chat_messages`, `faqs`). Scope lớn nhất. Có usecase spec trong `docs/usecase-specification/tri-tue-nhan-tao/`.
3. **Audit Log tối giản** — chưa có entity (`audit_logs`).
4. (Từ UC-38 A5) Dropdown 5 thông báo gần nhất + trang danh sách thông báo — vẫn ngoài scope; hiện chỉ có badge. Các route `/…/notifications` đang render `PlaceholderPage`.

## Files tham chiếu (đọc trước khi làm tiếp)

- `docs/handoff/project-progress.md` — checkpoint + quy ước (lưu ý: mục "Git status hiện tại" vẫn ghi HEAD cũ `8b94ffb`, đã lỗi thời — HEAD thật là `ddac218`).
- `docs/meeting-notes/session-2026-08-16.md` — Step 18.3 (đã hoàn thành).
- `docs/meeting-notes/README.md`.
- `database/schema.sql`, `docs/usecase-specification/`.

## Quy trình bắt buộc khi tiếp tục

1. Đọc handoff + project-progress.md trước.
2. Kiểm tra `git status` + `git rev-parse --short HEAD`.
3. Không tự ý code/commit; chờ người dùng chỉ định hạng mục.
4. Khi chọn hạng mục: audit schema/usecase/domain trước → đề xuất plan → chờ xác nhận → code → test/lint/build → commit (chỉ khi người dùng yêu cầu).