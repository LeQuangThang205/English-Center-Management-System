# Handoff — English Center Management System (Frontend Admin track)

Date (UTC): 2026-09-14
Workspace: `D:\English Center Management System`

## Where things stand

- S01 → S06 DONE (see `handoff-2026-09-12-s01-s06.md`).
- S07 Admin Attendance (`/admin/attendance`): DONE — READ-ONLY, 17/17 tests pass.
- Checkpoint 17.7 Verify Seed: VERIFIED — seed idempotent, notifications 6/6, backend 315/0/0.

## S07 detail (READ-ONLY, no write ops)

- Scope: xem sheets, filter lớp/ngày, xem records, counts PRESENT/ABSENT/EXCUSED. POST/PUT descoped (backend ADMIN có quyền nhưng use case Admin không yêu cầu); DELETE không tồn tại ở backend.
- `frontend/src/services/api/attendanceApi.ts` — chỉ thêm `getSheet(id)`; `getSheets({classId?,date?})` giữ nguyên.
- `frontend/src/features/attendance/useAdminAttendance.ts` (new) — `Promise.all([getClasses, getSheets])`, `classId null`/`date ''` → không gửi param, `selectedSheet` derive từ list (không N+1, modal không gọi detail).
- `frontend/src/pages/AdminAttendancePage.tsx` + `.module.css` (new) — PageHeader `Điểm danh`, toolbar (Select lớp + Input date + Xóa lọc), table Ngày/Lớp/Khóa học/Có mặt/Vắng/Phép/Tổng/Người tạo/Xem, modal `lg` records, Skeleton/ErrorState/EmptyState. Badge: PRESENT success / ABSENT danger / EXCUSED warning. `createdBy` null → `—`; sheet 0 records → counts 0.
- `frontend/src/routes/AppRoutes.tsx` — thêm `isAdminAttendance` → `AdminAttendancePage` (ADMIN-only). `navigation.ts` không sửa.
- `frontend/src/__tests__/adminAttendancePage.test.tsx` (new, 17 tests) — default GET không filter, refetch `?classId=`/`?date=`, cả hai params, clear filters, 2 empty states, error+retry, skeleton, modal từ list data (assert 0 request `/sheets/{id}`), 0 non-GET, không nút Tạo/Sửa/Xóa.

## 17.7 Verify Seed (VERIFY ONLY, no code changes)

- Seed cmd: `.\gradlew.bat bootRun --args="--spring.profiles.active=seed"` → profile `seed` active, MySQL OK, `Seed data already present; skipping` (idempotent, không duplicate).
- Notifications 6/6 đúng tiêu đề; recipients: student1 4 rows (unread 2), student4 3 rows, teacher1 2 rows, teacher2 2 rows, admin 0 rows (đúng — admin là người tạo). Đủ 4 target types (ALL_STUDENTS, ALL_TEACHERS, SPECIFIC_CLASS, SPECIFIC_USER), không orphan.
- Smoke API: login 3 role + `GET /notifications`, `/unread/count` đều 200 (server đã dừng sau verify, port 8080 free).
- Backend: `cleanTest test` → 315 tests / 0 failures / 0 errors / 0 skipped (23 classes).
- Ghi nhận (không blocker): 1 row `teacher1/#5` `isRead=True` khác seed ban đầu — runtime state (`findDetail`/`markAllAsRead` mới flip được), không phải lỗi seed.

## Working tree (all uncommitted, DO NOT commit/push/reset/revert)

Tracked modified:
- `frontend/src/routes/AppRoutes.tsx` (S07 route wiring)
- `frontend/src/services/api/attendanceApi.ts` (S07: +`getSheet`)

Untracked: `adminAttendancePage.test.tsx`, `features/attendance/`, `AdminAttendancePage.tsx/.module.css`.

## Verify status (last run, S07 + 17.7)

- S07 file: 17/17 PASS. Full frontend suite: 267/268 PASS.
- `npm run lint` PASS. `npm run build` PASS. `git diff --check` PASS.
- `git diff --name-only -- backend/ database/` → empty.

## Known issues (do NOT fix unless asked)

- `frontend/src/__tests__/dashboardCharts.test.tsx` — 1 failure (hard-coded `08/2026` / `3.300.000 ₫`). Unrelated backlog; leave alone.
- Backend backlog (từ S04): `POST /api/users` persists raw password (no `PasswordEncoder.encode`).

## Key contracts learned (don't re-audit unless needed)

- `GET /api/attendance/sheets` supports optional `?classId?&date?` (ADMIN sees all; TEACHER scoped; STUDENT 403). List response đã embed full `records` → detail modal dùng list data, không N+1.
- `AttendanceStatus`: PRESENT | ABSENT | EXCUSED. `AttendanceRecord` không có `note`. `PUT /sheets/{id}` chỉ replace `records` (không đổi class/date). Không có DELETE.
- `NotificationService.findAll/countUnread` là read thuần; chỉ `findDetail`/`markAllAsRead` flip `isRead`.
- `DataSeeder` idempotent qua `existsByEmail(admin@example.com)`; DB target `localhost:3306/english_center_db`.

## Next session focus

Awaiting user direction — likely S08. Keep enforcing: frontend-only, no backend/DB edits, no new dependencies, no commit/push, preserve working tree.

## Suggested skills

- `implement` — khi user order S08 (spec-driven implementation).
- `tdd` — nếu S08 muốn test-first.
- `to-spec` — nếu thảo luận scope mới cần capture thành spec.
- `handoff` — cuối phiên sau để compact tiếp.
