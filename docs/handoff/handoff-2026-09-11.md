# Handoff — 2026-09-11 (P0 Frontend S01–S04)

**Branch:** `main`
**HEAD:** `af912bb` (`update`) — không đổi trong phiên này
**Working tree:** `M frontend/src/routes/AppRoutes.tsx`, `M frontend/src/services/api/usersApi.ts` + 16 file mới untracked (S01–S04) + `?? .vs/` (IDE, bỏ qua)

> Quy tắc phiên: không commit, không push, không reset/revert. Backend/database diff rỗng suốt phiên.

## Trạng thái dự án

Hoàn thành **4 vertical slices frontend** (Wave A + 1 slice Wave B), tất cả chưa commit:

| Slice | Route | Files mới | Tests |
|-------|-------|-----------|-------|
| S01 Student Schedule | `/student/schedule` | `features/schedule/useStudentSchedule.ts`, `pages/StudentSchedulePage.tsx` + `.module.css` | `studentSchedulePage.test.tsx` — 10/10 PASS |
| S02 Student Courses | `/student/courses` | `features/courses/useStudentCourses.ts`, `pages/StudentCoursesPage.tsx` + `.module.css` | `studentCoursesPage.test.tsx` — 11/11 PASS |
| S03 Admin Scores | `/admin/scores` | `features/scores/useAdminScores.ts`, `pages/AdminScoresPage.tsx` + `.module.css` | `adminScoresPage.test.tsx` — 14/14 PASS |
| S04 Admin Students | `/admin/students` | `features/users/{useAdminStudents.ts,studentValidation.ts,StudentForm.tsx}`, `pages/AdminStudentsPage.tsx` + `.module.css` | `adminStudentsPage.test.tsx` — 15/15 PASS |

Files sửa: `services/api/usersApi.ts` (chỉ +`updateUser`/`deleteUser`, không `createUser`), `routes/AppRoutes.tsx` (wire 4 routes).

**Verify cuối phiên:** tổng frontend **212/213 PASS**, lint 0 error, build clean (1674 modules), `git diff --check` sạch.

## Contract backend đã xác minh (không suy luận lại)

- `GET /api/schedules` scope theo `@AuthenticationPrincipal` — không cần teacherId (`ScheduleController.java:26-36`).
- "Khóa học của tôi" = APPROVED **hoặc** PAID theo `ScheduleServiceImpl.java:34-39` (khớp `loadStudentDashboard.myCourses`).
- `RegistrationResponse` đã nhúng `className`/`courseName` — S02 không join `classesApi`.
- `GET /api/registrations` list **không check owner** (`RegistrationServiceImpl.java:176-187`) — frontend chỉ query own id.
- ADMIN scores: full read + filter `?classId=` (`ScoreServiceImpl.java:76-84`); total do backend tính.
- DELETE user = soft → INACTIVE; reactivate = PUT ACTIVE (`UserServiceImpl.java:80-85`).
- **CREATE descoped trong S04:** `POST /api/users` lưu `passwordHash` thô, không BCrypt (`UserController.java:30-38`, `UserServiceImpl.java:25-30`; encoder chỉ ở `AuthController.register:51`) → account tạo qua admin không đăng nhập được. Không UI tạo, không POST, không workaround auth/register (đã grep xác nhận vắng mặt).

## Backlog mở (chưa làm — chờ xác nhận riêng)

1. `dashboardCharts.test.tsx:320` — 1 fail có sẵn (dữ liệu 08/2026 cứng + real timers). Maintenance ticket riêng.
2. Backend: BCrypt-encode password ở path admin-create user (ticket sau P0).
3. Step 17.7 Verify Seed — NOT DONE (code 17.6 có, commit `61a7eae`; chưa có bằng chứng verify). Nên đóng trước Wave C live-verify.
4. P0 còn lại (Wave A→B→C): S05 Admin Teachers, S06 Admin Classes, S07 Admin Attendance (read), S08 Admin Registrations, S09 Admin Transactions, S10 Teacher Attendance, S11 Student Registrations.

## Quy trình bắt buộc khi tiếp tục

1. Đọc handoff này + kiểm tra `git status` + `git rev-parse --short HEAD`.
2. Không tự ý code/commit — mỗi slice: audit → plan → chờ APPROVE → implement → test/lint/build → báo cáo.
3. Không commit/push/reset/revert trừ khi người dùng lệnh rõ ràng.
4. Test pattern: `vi.stubGlobal('fetch', createFetchMock(...))`, luôn mock Topbar `/notifications/unread/count`, session qua `authStorage`, KHÔNG dùng `toBeInTheDocument()`.
5. Frontend-only cho đến khi P0 đóng; backend diff sau mỗi slice phải rỗng.
6. Lệnh verify trong `frontend/`: `npm run test -- --run <file>`, full `npm run test -- --run`, `npm run lint`, `npm run build`; root: `git diff --check`, `git status --short`, `git diff --name-only -- backend/ database/`.
