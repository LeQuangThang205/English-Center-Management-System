# Handoff — English Center Management System (Frontend Admin track)

Date (UTC): 2026-09-12
Workspace: `D:\English Center Management System`

## Where things stand

- S01 Student Schedule: DONE. S02 Student Courses: DONE. S03 Admin Scores: DONE.
- S04 Admin Students (`/admin/students`): DONE, APPROVED — 15/15 tests pass. CREATE STUDENT descoped (backend `POST /api/users` stores raw passwordHash without BCrypt — recorded as separate backend backlog).
- S05 Admin Teachers (`/admin/teachers`): DONE, APPROVED — 17/17 tests pass. CREATE TEACHER descoped (same backend password issue). No workaround used.
- S06 Admin Classes (`/admin/classes`): IMPLEMENTED, verify green, awaiting review/approve. Full CRUD (create/edit/cancel where DELETE = soft-cancel → CANCELLED per `CourseClassServiceImpl.delete`).

## Working tree (all uncommitted, DO NOT commit/push/reset/revert)

Tracked modified:
- `frontend/src/routes/AppRoutes.tsx` (S04–S06 route wiring)
- `frontend/src/services/api/usersApi.ts` (S04: UserPayload + update/delete)
- `frontend/src/services/api/classesApi.ts` (S06: ClassPayload + create/update/delete)

Untracked (new per step): S06 `ClassForm.tsx`, `classValidation.ts`, `useAdminClasses.ts`, `AdminClassesPage.tsx/.module.css`, `adminClassesPage.test.tsx`; S05 `TeacherForm.tsx`, `teacherValidation.ts`, `useAdminTeachers.ts`, `AdminTeachersPage.tsx/.module.css`, `adminTeachersPage.test.tsx`; S04/S03/S02/S01 files as before.

## Verify status (last run, S06)

- S06 file: 21/21 PASS. Full frontend suite: 250/251 PASS.
- `npm run lint` PASS. `npm run build` PASS. `git diff --check` PASS.
- `git diff --name-only -- backend/ database/` → empty (frontend-only, no backend/DB changes).

## Known issues (do NOT fix unless asked)

- `frontend/src/__tests__/dashboardCharts.test.tsx` — 1 failure ("renders the three charts…", hard-coded data/time). Unrelated maintenance backlog; leave alone in S06/S07.
- Backend backlog: `POST /api/users` persists raw password (no `PasswordEncoder.encode`, unlike `AuthController.register`). CREATE student/teacher descoped because of it.
- `CourseClassService.update` has no business validation and no status guards; FE mirrors create-rules + 2 documented FE-only guards (ACTIVE-only course dropdown, capacity ≥ headcount).

## Key contracts learned (don't re-audit unless needed)

- `GET /api/classes` query params are XOR (courseId > teacherId > status) — list calls it bare, filters client-side.
- `PUT /api/classes/{id}` has no `courseId` (immutable); `teacherId: null` clears assignment; `currentHeadcount` is read-only (managed by RegistrationService).
- Teacher = `User` with `role=TEACHER`; no TeacherProfile entity or specialty fields exist.
- Nav items for `/admin/*` already exist in `frontend/src/routes/navigation.ts`; only `AppRoutes.tsx` needs new branches.

## Next session focus

Awaiting user direction — likely S06 review result or S07. Keep enforcing: frontend-only, no backend/DB edits, no new dependencies, no commit/push, preserve working tree.

## Suggested skills

- `implement` — if user approves S06 and orders S07 build (spec-driven implementation).
- `tdd` — if next scope wants test-first development for a new admin page.
- `to-spec` — if discussion turns into a new scope (e.g., S07) that should be captured as a spec first.
- `diagnosing-bugs` — only if a real regression appears (not for the known dashboardCharts failure).
